# ==================== RTSP CAMERA CONFIGURATION ====================

# RTSP Stream URL (REQUIRED)
# Format: rtsp://username:password@ip_address:port/path
RTSP_URL=rtsp://admin:password@192.168.1.10

# ==================== OPTIONAL SETTINGS ====================
# Uncomment and adjust only if you need to override defaults
# The defaults work well for most setups - only change if experiencing issues

# RTSP Transport (default: tcp)
# tcp = More reliable, prevents HEVC errors (recommended)
# udp = Lower latency but may drop packets
#RTSP_TRANSPORT=tcp

# Buffer Size (default: 5 frames)
# Increase if you see HEVC "Could not find ref" errors
# Range: 1-10
#BUFFER_SIZE=5

# Ignore Regions (timestamp/watermark filtering)
# Format: x1,y1,x2,y2 (0.0-1.0 for percentages)
# Default masks bottom-left corner for timestamps
#IGNORE_REGIONS=0,0.85,0.45,1.0

# Timezone (default: Europe/Berlin for CET)
# Use IANA timezone names (e.g., Europe/Berlin, America/New_York, UTC)
# Applies to filenames and video overlays
#TIMEZONE=Europe/Berlin

# Fallback Motion Detection (default: false)
# NOT RECOMMENDED - causes false positives
# Keep disabled unless you have specific needs
#ENABLE_FALLBACK=false

# ==================== NOTES ====================
#
# Keyboard Controls (while running):
#   SPACE - Start/Stop manual recording
#   ENTER - Take screenshot
#   Q     - Quit
#
# Default Detection Settings:
#   - Confidence: 0.30 (filters false positives)
#   - Motion threshold: 75 pixels
#   - Sustained frames: 3 (requires consistent movement)
#
# To override defaults, use command-line arguments:
#   python motion_detector.py --confidence 0.25 --motion-threshold 50
#
# For help:
#   python motion_detector.py --help
